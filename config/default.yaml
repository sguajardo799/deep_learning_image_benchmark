mode: train_model
task: classification
dataset_name: timm/mini-imagenet
model_name: resnet18

paths:
  output_dir: ./data
  cache_dir: ./data/hf_cache
  save_path: ./data/best_model.pth
  metrics_path: ./data/training_metrics.csv
  summary_path: ./data/training_summary.json

train:
  batch_size: 8
  epochs: 2
  patience: 1
  lr: 0.001
